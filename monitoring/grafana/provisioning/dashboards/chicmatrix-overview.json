{
  "annotations": { "list": [] },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 0,
  "id": null,
  "links": [],
  "panels": [
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "reqps" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "id": 1,
      "title": "HTTP Requests",
      "type": "timeseries",
      "targets": [
        {
          "expr": "sum(rate(chicmatrix_http_requests_total[5m])) by (endpoint)",
          "legendFormat": "{{endpoint}}"
        }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "fieldConfig": { "defaults": { "unit": "s" }, "overrides": [] },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "id": 2,
      "title": "Request Latency (p95)",
      "type": "timeseries",
      "targets": [
        {
          "expr": "histogram_quantile(0.95, sum(rate(chicmatrix_http_request_duration_seconds_bucket[5m])) by (le, endpoint))",
          "legendFormat": "{{endpoint}}"
        }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 8 },
      "id": 3,
      "title": "Scrape Tasks",
      "type": "timeseries",
      "targets": [
        {
          "expr": "sum(increase(chicmatrix_scrape_tasks_total[1h])) by (status)",
          "legendFormat": "{{status}}"
        }
      ]
    },
    {
      "datasource": { "type": "prometheus", "uid": "prometheus" },
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 8 },
      "id": 4,
      "title": "Recommendations",
      "type": "timeseries",
      "targets": [
        {
          "expr": "sum(increase(chicmatrix_recommendations_total[1h]))",
          "legendFormat": "recommendations"
        }
      ]
    }
  ],
  "schemaVersion": 39,
  "tags": ["chicmatrix"],
  "templating": { "list": [] },
  "time": { "from": "now-1h", "to": "now" },
  "title": "ChicMatrix Overview",
  "uid": "chicmatrix-overview",
  "version": 1
}
