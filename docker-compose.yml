x-logging: &default-logging
  driver: json-file
  options:
    max-size: "10m"
    max-file: "3"

services:
  db:
    image: postgres:16-alpine
    container_name: chicmatrix-db
    environment:
      POSTGRES_USER: chicmatrix
      POSTGRES_PASSWORD: chicmatrix
      POSTGRES_DB: chicmatrix
    volumes:
      - postgres_data:/var/lib/postgresql/data
    ports:
      - "5433:5432"
    networks:
      - chicmatrix-internal
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U chicmatrix -d chicmatrix"]
      interval: 5s
      timeout: 5s
      retries: 10
    logging: *default-logging

  redis:
    image: redis:7-alpine
    container_name: chicmatrix-redis
    command: redis-server --appendonly yes
    volumes:
      - redis_data:/data
    networks:
      - chicmatrix-internal
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 10
    logging: *default-logging

  backend:
    build:
      context: .
      dockerfile: backend/Dockerfile
    container_name: chicmatrix-backend
    environment:
      DATABASE_URL: postgresql://chicmatrix:chicmatrix@db:5432/chicmatrix
      REDIS_URL: redis://redis:6379/0
      CELERY_BROKER_URL: redis://redis:6379/0
      CELERY_RESULT_BACKEND: redis://redis:6379/1
      CORS_ORIGINS: http://localhost:3000,http://localhost:3002,http://frontend:3000
      LOG_LEVEL: INFO
      FRONTEND_URL: http://localhost:3002
      API_BASE_URL: http://localhost:8001
      EMAIL_DEBUG: "true"
      CONSENT_VERSION: "1.0"
      SMS_DEBUG: "true"
      OTP_EXPIRE_MINUTES: "10"
      SOCIAL_AUTH_DEBUG: "true"
      JWT_SECRET_KEY: change-me-in-production
      JWT_ALGORITHM: HS256
      JWT_ACCESS_TOKEN_EXPIRE_MINUTES: "30"
      JWT_REFRESH_TOKEN_EXPIRE_DAYS: "7"
    ports:
      - "8001:8000"
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - chicmatrix-internal
      - chicmatrix-public
    logging: *default-logging

  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile
      args:
        NEXT_PUBLIC_API_URL: http://localhost:8001
        NEXT_PUBLIC_SOCIAL_AUTH_DEBUG: "true"
    container_name: chicmatrix-frontend
    environment:
      API_INTERNAL_URL: http://backend:8000
    ports:
      - "3002:3000"
    depends_on:
      - backend
    networks:
      - chicmatrix-internal
      - chicmatrix-public
    logging: *default-logging

  worker:
    build:
      context: .
      dockerfile: workers/Dockerfile
    environment:
      DATABASE_URL: postgresql://chicmatrix:chicmatrix@db:5432/chicmatrix
      CELERY_BROKER_URL: redis://redis:6379/0
      CELERY_RESULT_BACKEND: redis://redis:6379/1
      LOG_LEVEL: INFO
    depends_on:
      db:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - chicmatrix-internal
    logging: *default-logging

  flower:
    image: mher/flower:2.0.1
    container_name: chicmatrix-flower
    command: celery --broker=redis://redis:6379/0 flower --port=5555
    ports:
      - "5556:5555"
    depends_on:
      redis:
        condition: service_healthy
    networks:
      - chicmatrix-internal
      - chicmatrix-public
    logging: *default-logging

  prometheus:
    image: prom/prometheus:v2.54.1
    container_name: chicmatrix-prometheus
    volumes:
      - ./monitoring/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prometheus_data:/prometheus
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
      - "--storage.tsdb.path=/prometheus"
    ports:
      - "9090:9090"
    networks:
      - chicmatrix-internal
      - chicmatrix-public
    logging: *default-logging

  grafana:
    image: grafana/grafana:11.3.1
    container_name: chicmatrix-grafana
    environment:
      GF_SECURITY_ADMIN_USER: admin
      GF_SECURITY_ADMIN_PASSWORD: chicmatrix
      GF_USERS_ALLOW_SIGN_UP: "false"
    volumes:
      - grafana_data:/var/lib/grafana
      - ./monitoring/grafana/provisioning:/etc/grafana/provisioning:ro
    ports:
      - "3001:3000"
    depends_on:
      - prometheus
    networks:
      - chicmatrix-public
    logging: *default-logging

networks:
  chicmatrix-internal:
    driver: bridge
  chicmatrix-public:
    driver: bridge

volumes:
  postgres_data:
  redis_data:
  prometheus_data:
  grafana_data:
